mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
            // 주소는 express가 data에 임의로 부여한 id로 이동
            // Attribute에서는 backtick 사용
        p=video.description
        ul
            each hashtag in video.hashtags
                li=hashtag
        small=video.createdAt
        hr 
        // Variable과 String을 섞어서 작성하는 방식